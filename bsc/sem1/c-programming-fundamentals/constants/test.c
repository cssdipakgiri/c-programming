#include<stdio.h>

void main(){
    const float VALUE_OF_PI = 3.14343287;

    // CONSTANT variables are recomended to be in capital letters
    // the value stored in it should not be changed. 

    printf("value of constant VALUE_OF_PI is %0.3f ", VALUE_OF_PI);

    // IMPORTANT: the 0.3 or 0.n in genarel means that, we want to print the float till 3 or  n decimal points.

}